#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int>v;

    v.push_back(2);
    v.push_back(3);
    v.push_back(5);
    v.push_back(7);
    for (int i = 0; i < 4; i++)
    {
        cout << v[i] << ' ';
    }
    cout << endl;
    
    return 0;
}
// output:
// 2 3 5 7