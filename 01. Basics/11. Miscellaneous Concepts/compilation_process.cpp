//  compilation_process.cpp – How C++ compiles and runs

#include <iostream>
using namespace std;

int main() {
    cout << "Understanding C++ Compilation Process" << endl;

    /*
    C++ Compilation Stages:

    1. Preprocessing:
       - Handles directives like #include, #define
       - Removes comments
       - Expands macros

    2. Compilation:
       - Converts code to assembly
       - Syntax and semantic checks

    3. Assembling:
       - Converts assembly code to machine code (object files)

    4. Linking:
       - Links multiple object files and libraries
       - Produces final executable

    Example:
    g++ compilation_process.cpp -o output

    Output:
    Understanding C++ Compilation Process
    */

    return 0;
}
